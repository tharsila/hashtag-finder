export interface IUser {
    name: string;
    description: string;
    github: string;
    email: string;
    linkedin: string;
    img: string;
}

type User = {
    Email: string;
    Senha: string;
}
export default User;